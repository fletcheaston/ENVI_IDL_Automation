# Complete!

Task = ENVITask('BuildMosaicRaster')

# All the raster files in an array. Uses the FID, and ENVIFIDToRaster().
Task.INPUT_RASTERS = [{inputRasters}]

# Color matching actions are all 'Adjust' except the last one, which is 'Reference'.
Task.COLOR_MATCHING_ACTIONS = [{colorMatchingActions}]

# We don't use any histogram color matching.
Task.COLOR_MATCHING_METHOD = 'None'

# Background is the data ignore value. In ENVI 5.3, the data ignore value is referenced using BACKGROUND. In later versions, the data ignore value is referenced using DATA_IGNORE_VALUE.
Task.BACKGROUND = 0

# Feathering distance is an array, all values are 5.
Task.FEATHERING_DISTANCE = [{featheringDistance}]

# Determines how the rasters are meshed together.
Task.FEATHERING_METHOD = 'Seamline'
Task.SEAMLINE_METHOD = 'Geometry'

# Runs the task.
Task.Execute

{mosaicRaster} = Task.OUTPUT_RASTER

# Adds the mosaic to the ENVI viewable interface.
View = E.GetView()
Layer = View.CreateLayer(Task.OUTPUT_RASTER)